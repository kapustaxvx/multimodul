service:
  message: Hello, World

server:
  port: 9091

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tasklistDB
    username: taskadmin
    password: admin
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
    schema: classpath*:database/initDB.sql